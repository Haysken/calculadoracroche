HTML

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de Crochê</title>
    <style>
        /* Estilos (CSS) - A aparência do site */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #fdf2f8; /* Rosa bem clarinho */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .calculadora-card {
            background-color: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
            border-top: 5px solid #db2777; /* Rosa escuro */
        }

        h1 {
            color: #db2777;
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
        }

        .input-group {
            margin-bottom: 1rem;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            color: #4b5563;
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 10px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 1rem;
            box-sizing: border-box; /* Garante que o padding não aumente a largura */
        }

        input:focus {
            border-color: #db2777;
            outline: none;
        }

        button {
            background-color: #db2777;
            color: white;
            border: none;
            padding: 12px;
            width: 100%;
            border-radius: 8px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.3s;
            margin-top: 10px;
        }

        button:hover {
            background-color: #be185d;
        }

        #resultado {
            margin-top: 20px;
            padding: 15px;
            background-color: #fce7f3;
            border-radius: 8px;
            display: none; /* Escondido até calcular */
        }

        .preco-final {
            font-size: 2rem;
            font-weight: bold;
            color: #db2777;
        }
        
        .detalhes {
            font-size: 0.9rem;
            color: #666;
            margin-top: 5px;
            text-align: left;
        }
    </style>
</head>
<body>

    <div class="calculadora-card">
        <h1>🧶 Precificação de Crochê</h1>
        
        <div class="input-group">
            <label for="custoLinhas">Custo total das linhas (R$)</label>
            <input type="number" id="custoLinhas" placeholder="Ex: 350.00" step="0.01">
        </div>

        <div class="input-group">
            <label for="dificuldade">Nível de Dificuldade (0 a 10)</label>
            <input type="number" id="dificuldade" placeholder="0 a 10" min="0" max="10">
        </div>

        <button onclick="calcularPreco()">Calcular Preço</button>

        <div id="resultado">
            <div>Preço sugerido:</div>
            <div class="preco-final" id="valorFinal">R$ 0,00</div>
            <hr style="border: 0; border-top: 1px solid #db2777; margin: 10px 0;">
            <div class="detalhes" id="detalhesCalculo">
                </div>
        </div>
    </div>

    <script>
        // Lógica (JavaScript) - O cérebro da calculadora
        function calcularPreco() {
            // 1. Pegar os valores dos inputs
            let custo = parseFloat(document.getElementById('custoLinhas').value);
            let dificuldade = parseInt(document.getElementById('dificuldade').value);

            // Validação simples
            if (isNaN(custo) || isNaN(dificuldade)) {
                alert("Por favor, preencha todos os campos corretamente.");
                return;
            }

            if (dificuldade < 0 || dificuldade > 10) {
                alert("A dificuldade deve ser entre 0 e 10.");
                return;
            }

            // 2. Aplicar a fórmula
            // Preço = 2 * custo + 30 * dificuldade
            let valorBase = custo * 2;
            let valorMaoDeObra = dificuldade * 30;
            let precoTotal = valorBase + valorMaoDeObra;

            // 3. Mostrar o resultado na tela
            const divResultado = document.getElementById('resultado');
            const spanValorFinal = document.getElementById('valorFinal');
            const divDetalhes = document.getElementById('detalhesCalculo');

            // Formatando para Dinheiro Brasileiro (R$)
            let formatoMoeda = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });

            spanValorFinal.innerText = formatoMoeda.format(precoTotal);
            
            divDetalhes.innerHTML = `
                1️⃣ Base (Material + Lucro): <b>${formatoMoeda.format(valorBase)}</b><br>
                2️⃣ Mão de obra (Dificuldade): <b>${formatoMoeda.format(valorMaoDeObra)}</b>
            `;

            divResultado.style.display = 'block';
        }
    </script>
</body>
</html>
